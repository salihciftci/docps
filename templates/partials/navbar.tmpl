<nav class="navbar navbar-expand-lg navbar-white bg-white">
    <ul class="navbar-nav mr-auto">
        {{!-- <div style={{ fontWeight: "bold", textTransform: "capitalize" }} onClick={this.props.handleClick}>
            {this.props.title}</div> --}}
        <div>Title</div>
    </ul>
    <div class="form-inline my-2">
        <button class="btn avatar" type="button" {{!--style={{ border: "1px solid #f0f0f0", overflow: "hidden" }} --}}>
            <i class="far fa-bell"></i>
        </button>
        <button class="btn" type="button">
            <img src="https://www.gravatar.com/avatar/6b890c9fc3ba836945c5538583bf6369" alt="avatar" class="avatar" />
        </button>
    </div>
</nav>
